<% if (email.address == email.invoice.owner.email) { %>
✔ <%= email.invoice.payer.email %> оплатил счет на сумму <%= email.invoice.amount %> руб.
<% } else { %>
✔ Вы оплатили счет от <%= email.invoice.owner.email %> на сумму <%= email.invoice.amount %> руб.
<% } %>