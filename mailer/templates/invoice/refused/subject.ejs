<% if (email.address == email.invoice.owner.email) { %>
✘ <%= email.invoice.payer.email %> отказался от оплаты <%= email.invoice.amount %> руб.
<% } else { %>
✘ Вы отказались оплачивать счет от <%= email.invoice.owner.email %> на сумму <%= email.invoice.amount %> руб.
<% } %>