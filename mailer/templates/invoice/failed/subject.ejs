<% if (email.address == email.invoice.owner.email) { %>
⚠ <%= email.invoice.payer.email %> не смог оплатить счет на сумму <%= email.invoice.amount %> руб.
<% } else { %>
⚠ Произошла ошибка при оплате счета от <%= email.invoice.owner.email %> на сумму <%= email.invoice.amount %> руб.
<% } %>